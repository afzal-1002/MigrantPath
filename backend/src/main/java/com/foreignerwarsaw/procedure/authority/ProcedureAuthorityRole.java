package com.foreignerwarsaw.procedure.authority;

/** Mirrors {@code procedure_authorities.role}'s CHECK constraint (V32, brief §30). */
public enum ProcedureAuthorityRole {
  LEGAL_AUTHORITY,
  PROCESSING_AUTHORITY,
  MUNICIPAL_AUTHORITY,
  INFORMATION_AUTHORITY
}
