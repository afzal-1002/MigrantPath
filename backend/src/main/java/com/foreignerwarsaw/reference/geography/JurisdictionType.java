package com.foreignerwarsaw.reference.geography;

/** Mirrors the CHECK constraint on {@code jurisdictions.jurisdiction_type} (V14). */
public enum JurisdictionType {
  NATIONAL,
  REGIONAL,
  MUNICIPAL
}
