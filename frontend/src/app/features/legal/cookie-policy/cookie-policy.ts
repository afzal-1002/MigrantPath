import { Component } from '@angular/core';

/** Phase 11 brief §192 - draft cookie page, content traces directly to
 * docs/privacy/COOKIE_INVENTORY.md (the same two real cookies, no invented ones). */
@Component({
  selector: 'app-cookie-policy',
  imports: [],
  templateUrl: './cookie-policy.html',
  styleUrl: '../legal-page.scss',
})
export class CookiePolicy {}
