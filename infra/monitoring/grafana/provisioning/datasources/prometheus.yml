# Canonical Phase 14 (Observability) - local-only, provisions the Prometheus
# datasource automatically so a fresh `docker compose up` needs no manual setup.
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://prometheus:9090
    isDefault: true
    editable: false
